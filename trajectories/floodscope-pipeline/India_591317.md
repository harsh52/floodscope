# Trajectory — floodscope-pipeline — India_591317

- **workflow_id:** `floodscope-pipeline:India_591317`
- **events:** 12

---

### `analyze` · System prompt
```
Deterministic flood-mapping pipeline. Classify open water in a Sentinel-1 VV/VH scene using verification-gated thresholding and terrain-aware cleanup.
```

### `analyze` · User prompt
```
Map flood water for chip India_591317 (second india biome).
```

**→ tool call** `speckle({"detail": "speckle(median5)"})`

**← tool result** ✓ `speckle`: speckle(median5)

**→ tool call** `threshold({"detail": "threshold(gated-otsu, -21.15 dB)"})`

**← tool result** ✓ `threshold`: threshold(gated-otsu, -21.15 dB)

**→ tool call** `cleanup({"detail": "cleanup(min=10, -115 px)"})`

**← tool result** ✓ `cleanup`: cleanup(min=10, -115 px)

**Verification ✅ passed**
```json
{
  "threshold_method": "gated-otsu",
  "bimodal_histogram": true,
  "note": "Otsu trusted only on genuinely bimodal scenes; else fixed dB fallback."
}
```

**🧑‍⚖️ human review: pending** — flood map awaiting analyst sign-off before publication

**Checkpoint**
```json
{
  "metrics": {
    "iou": 0.8777,
    "precision": 0.9304,
    "recall": 0.9393,
    "f1": 0.9349,
    "tp": 107118,
    "fp": 8009,
    "fn": 6921,
    "tn": 124001,
    "pred_water_px": 115127,
    "true_water_px": 114039,
    "area_km2": 11.51,
    "area_pct": 46.8,
    "threshold_method": "gated-otsu",
    "threshold_db": -21.15
  }
}
```

_— analyze complete —_
