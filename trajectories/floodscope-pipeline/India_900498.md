# Trajectory — floodscope-pipeline — India_900498

- **workflow_id:** `floodscope-pipeline:India_900498`
- **events:** 12

---

### `analyze` · System prompt
```
Deterministic flood-mapping pipeline. Classify open water in a Sentinel-1 VV/VH scene using verification-gated thresholding and terrain-aware cleanup.
```

### `analyze` · User prompt
```
Map flood water for chip India_900498 (monsoon inundation).
```

**→ tool call** `speckle({"detail": "speckle(median5)"})`

**← tool result** ✓ `speckle`: speckle(median5)

**→ tool call** `threshold({"detail": "threshold(gated-otsu, -21.02 dB)"})`

**← tool result** ✓ `threshold`: threshold(gated-otsu, -21.02 dB)

**→ tool call** `cleanup({"detail": "cleanup(min=10, -241 px)"})`

**← tool result** ✓ `cleanup`: cleanup(min=10, -241 px)

**Verification ✅ passed**
```json
{
  "threshold_method": "gated-otsu",
  "bimodal_histogram": true,
  "note": "Otsu trusted only on genuinely bimodal scenes; else fixed dB fallback."
}
```

**🧑‍⚖️ human review: pending** — flood map awaiting analyst sign-off before publication

**Checkpoint**
```json
{
  "metrics": {
    "iou": 0.7607,
    "precision": 0.845,
    "recall": 0.8841,
    "f1": 0.8641,
    "tp": 109583,
    "fp": 20106,
    "fn": 14370,
    "tn": 117933,
    "pred_water_px": 129689,
    "true_water_px": 123953,
    "area_km2": 12.97,
    "area_pct": 49.5,
    "threshold_method": "gated-otsu",
    "threshold_db": -21.02
  }
}
```

_— analyze complete —_
