# Trajectory — floodscope-pipeline — Mekong_333434

- **workflow_id:** `floodscope-pipeline:Mekong_333434`
- **events:** 12

---

### `analyze` · System prompt
```
Deterministic flood-mapping pipeline. Classify open water in a Sentinel-1 VV/VH scene using verification-gated thresholding and terrain-aware cleanup.
```

### `analyze` · User prompt
```
Map flood water for chip Mekong_333434 (delta / rice paddy).
```

**→ tool call** `speckle({"detail": "speckle(median5)"})`

**← tool result** ✓ `speckle`: speckle(median5)

**→ tool call** `threshold({"detail": "threshold(gated-otsu, -16.47 dB)"})`

**← tool result** ✓ `threshold`: threshold(gated-otsu, -16.47 dB)

**→ tool call** `cleanup({"detail": "cleanup(min=10, -2289 px)"})`

**← tool result** ✓ `cleanup`: cleanup(min=10, -2289 px)

**Verification ✅ passed**
```json
{
  "threshold_method": "gated-otsu",
  "bimodal_histogram": true,
  "note": "Otsu trusted only on genuinely bimodal scenes; else fixed dB fallback."
}
```

**🧑‍⚖️ human review: pending** — flood map awaiting analyst sign-off before publication

**Checkpoint**
```json
{
  "metrics": {
    "iou": 0.0516,
    "precision": 0.0524,
    "recall": 0.7798,
    "f1": 0.0982,
    "tp": 2561,
    "fp": 46315,
    "fn": 723,
    "tn": 153534,
    "pred_water_px": 48876,
    "true_water_px": 3284,
    "area_km2": 4.89,
    "area_pct": 24.1,
    "threshold_method": "gated-otsu",
    "threshold_db": -16.47
  }
}
```

_— analyze complete —_
