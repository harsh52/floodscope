# Trajectory — floodscope-pipeline — USA_905409

- **workflow_id:** `floodscope-pipeline:USA_905409`
- **events:** 12

---

### `analyze` · System prompt
```
Deterministic flood-mapping pipeline. Classify open water in a Sentinel-1 VV/VH scene using verification-gated thresholding and terrain-aware cleanup.
```

### `analyze` · User prompt
```
Map flood water for chip USA_905409 (midwest river flood).
```

**→ tool call** `speckle({"detail": "speckle(median5)"})`

**← tool result** ✓ `speckle`: speckle(median5)

**→ tool call** `threshold({"detail": "threshold(gated-otsu, -23.52 dB)"})`

**← tool result** ✓ `threshold`: threshold(gated-otsu, -23.52 dB)

**→ tool call** `cleanup({"detail": "cleanup(min=10, -113 px)"})`

**← tool result** ✓ `cleanup`: cleanup(min=10, -113 px)

**Verification ✅ passed**
```json
{
  "threshold_method": "gated-otsu",
  "bimodal_histogram": true,
  "note": "Otsu trusted only on genuinely bimodal scenes; else fixed dB fallback."
}
```

**🧑‍⚖️ human review: pending** — flood map awaiting analyst sign-off before publication

**Checkpoint**
```json
{
  "metrics": {
    "iou": 0.9197,
    "precision": 0.9932,
    "recall": 0.9255,
    "f1": 0.9582,
    "tp": 41155,
    "fp": 283,
    "fn": 3312,
    "tn": 217349,
    "pred_water_px": 41438,
    "true_water_px": 44467,
    "area_km2": 4.14,
    "area_pct": 15.8,
    "threshold_method": "gated-otsu",
    "threshold_db": -23.52
  }
}
```

_— analyze complete —_
